# Dataset

Place the project dataset here as:

```text
data/knowledge_base.csv
```

Expected columns:

```text
document_id
category
title
content
keywords
```

The dataset itself is intentionally not bundled in this template.

After adding the CSV, run:

```bash
python scripts/train_models.py
```
